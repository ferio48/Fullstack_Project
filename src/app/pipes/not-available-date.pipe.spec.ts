import { NotAvailableDatePipe } from './not-available-date.pipe';

describe('NotAvailableDatePipe', () => {
  it('create an instance', () => {
    const pipe = new NotAvailableDatePipe();
    expect(pipe).toBeTruthy();
  });
});
