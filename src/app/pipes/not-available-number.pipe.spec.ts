import { NotAvailableNumberPipe } from './not-available-number.pipe';

describe('NotAvailableNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new NotAvailableNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
